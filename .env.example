DATABASE_URL=sqlite:data.db
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Geocoding (optional — providers are tried in order: Census -> Nominatim -> Google Maps)
# NOTE: as of this build, all three providers in geo-enrich are stubs
# (see IMPLEMENTATION_NOTES.md) — these flags are read but not yet acted on.
GEOCODING_CENSUS_ENABLED=true
GEOCODING_NOMINATIM_ENABLED=true
GEOCODING_GOOGLE_MAPS_ENABLED=false
GEOCODING_GOOGLE_MAPS_API_KEY=

# Probe settings (compliance-probe is a stub — see IMPLEMENTATION_NOTES.md)
PROBE_CONCURRENCY=10
PROBE_RATE_LIMIT_PER_SEC=5.0
